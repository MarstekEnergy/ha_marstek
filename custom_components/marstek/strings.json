{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    },
    "error": {
      "cannot_connect": "Unable to connect to the device",
      "device_not_found": "Device not found at the specified IP address",
      "discovery_failed": "Failed to discover devices. Please check your network connection and try again.",
      "no_devices_found": "No Marstek devices found on the network. Please ensure your device is powered on and connected to the same network."
    },
    "step": {
      "user": {
        "data": {
          "device": "Select device"
        },
        "data_description": {
          "device": "Choose the Marstek device you want to add"
        },
        "description": "Select your Marstek energy storage device from the discovered devices",
        "title": "Marstek energy storage"
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Marstek communication",
        "description": "Tune polling to match your network. Do not poll faster than every 10 seconds. A 4 second delay between UDP requests is recommended on Wi-Fi.",
        "data": {
          "scan_interval": "Fast polling interval (seconds)",
          "medium_scan_interval": "Medium polling interval (seconds)",
          "request_delay": "Delay between UDP requests (seconds)",
          "request_timeout": "Request timeout (seconds)",
          "failures_before_unavailable": "Failures before unavailable"
        },
        "data_description": {
          "scan_interval": "How often ES.GetMode and PV.GetStatus are polled (default: 30 s). Minimum: 10 s.",
          "medium_scan_interval": "How often ES.GetStatus energy counters are refreshed (default: 300 s).",
          "request_delay": "Pause between consecutive API calls within one poll cycle (default: 4 s).",
          "request_timeout": "Per-request UDP timeout before retry (default: 5 s).",
          "failures_before_unavailable": "Consecutive failed polls before entities become unavailable (default: 3)."
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "battery_level": {
        "name": "Battery level"
      }
    }
  }
}
